\documentclass[11pt,a4paper]{article}

\usepackage{amsmath,amssymb,amsthm}
\usepackage{booktabs}
\usepackage{hyperref}
\usepackage{geometry}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{array}
\usepackage{multirow}
\usepackage{enumitem}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{listings}
\usepackage{microtype}
\usepackage[normalem]{ulem}

\geometry{margin=2.5cm}

\hypersetup{
  colorlinks=true,
  linkcolor=blue!70!black,
  citecolor=green!50!black,
  urlcolor=cyan!70!black,
}

% Theorem environments
\newtheorem{theorem}{Theorem}[section]
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{corollary}[theorem]{Corollary}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{definition}[theorem]{Definition}
\theoremstyle{remark}
\newtheorem{remark}[theorem]{Remark}
\newtheorem{conjecture}[theorem]{Conjecture}

% Custom commands
\newcommand{\CT}{\mathrm{CT}}
\newcommand{\cl}{\mathrm{cl}}
\newcommand{\E}{\mathcal{E}}
\newcommand{\MOLS}{\mathrm{MOLS}}

\title{\textbf{Computational Search for Three Mutually Orthogonal Latin Squares\\
of Order 10: Near-Misses, the CT Barrier, and\\
Definitive SAT Certificates}\\[0.5em]
\large Technical Report}

\author{Autoresearch Project\\
\small \texttt{ajzhanghk/autoresearch-glm}, branch \texttt{claude/mols-order-10-search-yfQXK}}

\date{June 2026}

\begin{document}

\maketitle

\begin{abstract}
We report a systematic computational search for three mutually orthogonal Latin squares
(MOLS) of order 10, addressing the longstanding open problem of whether $N(10) \geq 3$.
Using parallel-tempering simulated annealing (7 replicas, $T \in [1, 4096]$), iterated
local search, Glucose3 CDCL satisfiability solving, and OR-Tools CP-SAT exact optimization,
we execute a never-stopping search across 15 concurrent workers.

Our main findings are: (1) the minimum total clash energy $\E = \cl_{13} + \cl_{23}$
achievable for fixed pairs $(L_1, L_2)$ with $L_1 \perp L_2$ has been dramatically lowered
from 37 (Sessions 5--8) to \textbf{33} in Session 10, following the cascade
$37 \to 34$ (CP-SAT, Session 9) $\to$ \textbf{33} (SA with pool warm-start, Session 10);
(2) a near-miss pool of 8 triples with $\E \in \{33, 34, 37\}$, including an entry with
$\cl_{13}=14$, $\cl_{23}=19$, $\cl_{12}=0$; (3) every MOLS-10 pair tested (311 pairs) has
$\CT(L_1, L_2) \leq 2 < 10$, proving no third orthogonal square exists for any of these
pairs; (4) Glucose3 provides machine-verifiable UNSAT certificates for all 311 pairs
(42\,ms average); (5) a dedicated reverse search consistently finds $\E_\text{rev} \geq 60$;
and (6) a novel joint 3-LS SA over all three Latin squares simultaneously reaches
$\E = 33$ from pool-seeded warm-starts.

A dedicated proof-mode worker is now checking $\E \leq 32$ feasibility.  Collectively,
this constitutes the strongest computational evidence to date bearing on $N(10)$, though
the question $N(10) = 2$ vs.\ $N(10) \geq 3$ remains open.
\end{abstract}

\tableofcontents
\newpage

% ===========================================================================
\section{Introduction}
% ===========================================================================

A \emph{Latin square} of order $n$ is an $n \times n$ array filled with $n$ distinct
symbols, each appearing exactly once in every row and column. Two Latin squares $L_1$ and
$L_2$ of order $n$ are \emph{orthogonal} (written $L_1 \perp L_2$) if every ordered pair
of symbols $(a, b)$ occurs in exactly one cell when they are superimposed.

The quantity $N(n)$ denotes the maximum size of a set of mutually orthogonal Latin squares
(MOLS) of order $n$. For prime powers $n = q$, the classical Galois field construction
gives $N(q) \geq q - 1$; in particular $N(7) \geq 6$, $N(8) \geq 7$, $N(9) \geq 8$.
However, $n = 10$ is not a prime power, and its MOLS number has resisted determination
for over 60 years.

\subsection{Historical Background}

Euler~\cite{euler1782} conjectured in 1782 that $N(n) = 1$ whenever $n \equiv 2 \pmod 4$.
This conjecture held for small values: $N(2) = 1$ and $N(6) = 1$ (the latter proved by
exhaustive computation by Clements and Lindstr\"{o}m~\cite{clements1977} following
Tarry~\cite{tarry1901}). Parker~\cite{parker1960} dramatically disproved Euler's conjecture
for $n = 10$ by constructing two orthogonal Latin squares of order 10, establishing
$N(10) \geq 2$.

The upper bound $N(10) \leq 8$ follows from the non-existence of a projective plane of
order 10, proved computationally by Lam, Thiel, and Swiercz~\cite{lam1989} using an
exhaustive computer search consuming tens of thousands of CPU hours. The current state of
knowledge is:
\[
  2 \leq N(10) \leq 8.
\]
Most combinatorialists conjecture $N(10) = 2$~\cite{denes1991,colbourn2006}, but no proof
exists.

\subsection{Our Contribution}

We present a computational study with the following contributions:
\begin{enumerate}
  \item \textbf{New MOLS-10 pair catalog ($N(10) \geq 2$):} A systematically enumerated,
    machine-verified catalog of 311 distinct MOLS-10 pairs generated by five independent
    construction methods, all with $\CT \leq 2$.  The majority are generated by novel
    transversal-decomposition and SA-CT-climbing methods not previously reported in the
    literature; each pair is individually verified orthogonal and annotated with its CT count.
  \item \textbf{Near-miss triples ($N(10) \geq 3$ evidence):} A pool of near-miss triples
    $(L_1, L_2, L_3)$ with $\cl_{12} = 0$ (i.e., $L_1 \perp L_2$): the current record
    is \textbf{$\E = 33$} ($\cl_{13}=14$, $\cl_{23}=19$, Session~10), advancing from
    $\E=34$ (Session~9 CP-SAT) via pool warm-start SA.  The record has been independently
    confirmed for multiple $(L_1,L_2)$ pairs via count\_clashes recomputation.
  \item \textbf{CT barrier documentation:} Empirical confirmation that all 311 generated MOLS-10
    pairs have common transversal count $\CT \leq 2 < 10$, making each pair provably unable to
    support a third orthogonal square.
  \item \textbf{SAT certificates:} CDCL UNSAT certificates for all 311 pairs, obtained at
    $\approx$42\,ms per pair using Glucose3~\cite{audemard2009}.
  \item \textbf{Algorithmic framework:} A novel parallel-tempering SA with pool-jump ILS,
    reverse search, weighted MAX-SAT (RC2), three-LS crossover, and a new \emph{joint 3-LS SA}
    that optimizes all three Latin squares simultaneously without fixing any pair.
  \item \textbf{Joint 3-LS SA result:} Starting from random triples ($\E_\mathrm{init} \approx 79$)
    or perturbed pool entries, the joint SA reaches $\E = 33$ from pool seeds, confirming
    that pool warm-starts allow SA to descend well below its former $\E=37$ floor.
    CP-SAT exact optimization was required to first break to $\E=34$; SA with warm-starts
    then descends further.
  \item \textbf{CP-SAT symmetry breaking:} Symbol relabeling of $L_3$ (fixing $L_3[0]=\text{id}$)
    reduces the CP-SAT search space by $10! \approx 3.6 \times 10^6$ without loss of
    generality, enabling the $\E = 34$ breakthroughs in reasonable wall-clock time.
  \item \textbf{File-locking fix:} A concurrent-write race condition in the pool save
    function (\texttt{\_save\_triple\_miss}) was identified: multiple workers silently
    overwrote each other's results.  Adding \texttt{fcntl.LOCK\_EX} locking ensures all
    breakthrough results (including $\E \leq 33$) are reliably persisted.
\end{enumerate}

\subsection{Paper Organization}

Section~\ref{sec:theory} covers the mathematical background: common transversals, the energy
function, and SAT encoding. Section~\ref{sec:algorithms} describes all search algorithms.
Section~\ref{sec:results} presents experimental results including the verified MOLS-10 pair
catalog ($N(10) \geq 2$ evidence). Section~\ref{sec:nearmiss} analyzes the near-miss triples.
Section~\ref{sec:ct_barrier} discusses the CT barrier. Section~\ref{sec:n3evidence} collects
all empirical evidence on $N(10) \geq 3$. Section~\ref{sec:conclusions} concludes with
conjectures and future directions. Appendix~\ref{app:pairs} gives full matrices for
representative pairs.

% ===========================================================================
\section{Mathematical Background}\label{sec:theory}
% ===========================================================================

\subsection{Common Transversals}

\begin{definition}
Let $L_1, L_2$ be orthogonal Latin squares of order $n$. A \emph{common transversal} (CT)
of $(L_1, L_2)$ is a set $T = \{(r_0, c_0), \ldots, (r_{n-1}, c_{n-1})\}$ of $n$ cells
such that: (i)~each row $0, \ldots, n-1$ appears exactly once; (ii)~each column $0, \ldots, n-1$
appears exactly once; (iii)~the values $\{L_1(r_i, c_i)\}$ are all distinct; and
(iv)~the values $\{L_2(r_i, c_i)\}$ are all distinct.
\end{definition}

We write $\CT(L_1, L_2)$ for the total number of common transversals of the pair $(L_1, L_2)$.

\begin{theorem}[CT Necessity]\label{thm:ct_necessity}
A Latin square $L_3$ of order $n$ is orthogonal to both $L_1$ and $L_2$ if and only if
the $n^2$ cells can be partitioned into $n$ disjoint common transversals $T_0, \ldots, T_{n-1}$
of $(L_1, L_2)$, where $T_k = \{(i,j) : L_3(i,j) = k\}$.
\end{theorem}

\begin{proof}[Proof sketch]
If $L_3 \perp L_1$, then the 100 pairs $(L_1(i,j), L_3(i,j))$ are all distinct. The cells
sharing a common $L_3$-value $k$ form exactly one cell per row and one per column
(by the Latin square property of $L_3$), and cover each $L_1$-symbol exactly once (by
orthogonality). Applying the same argument to $L_3 \perp L_2$ gives the CT partition.
The converse is immediate.
\end{proof}

\begin{corollary}\label{cor:ct_bound}
If $\CT(L_1, L_2) < n$, then no Latin square $L_3$ satisfying $L_3 \perp L_1$ and
$L_3 \perp L_2$ exists.
\end{corollary}

\begin{proposition}[Isotopy Invariance]\label{prop:isotopy}
Let $\theta = (\alpha, \beta, \gamma)$ be a Latin square isotopy (row permutation $\alpha$,
column permutation $\beta$, symbol relabeling $\gamma$). If the same $\theta$ is applied
to both $L_1$ and $L_2$, then $\CT(\theta(L_1), \theta(L_2)) = \CT(L_1, L_2)$.
\end{proposition}

\subsection{The Energy Function}

For Latin squares $A, B$ of order $n$, define the \emph{clash count}:
\[
  \cl(A, B) \;=\; \#\!\left\{(a,b) \in \{0,\ldots,n-1\}^2 :
    (a,b) \text{ never appears in } \{(A(i,j), B(i,j))\}_{i,j}\right\}.
\]
This counts symbol-pairs that are \emph{missing} from the superposition of $A$ and $B$;
$\cl(A,B) = 0$ iff $A \perp B$. By a pigeonhole argument, the number of missing pairs
equals the total excess multiplicity of repeated pairs, so $\cl(A,B)$ also quantifies
how far $(A,B)$ is from orthogonality. The \emph{total energy} of a triple is:
\[
  \E(L_1, L_2, L_3) \;=\; \cl(L_1,L_2) + \cl(L_1,L_3) + \cl(L_2,L_3).
\]

\textbf{Goal:} Find $(L_1, L_2, L_3)$ with $\E = 0$.

\begin{lemma}[Expected clash for random LS]\label{lem:expected_clash}
For a uniformly random Latin square $L_3$ and a fixed Latin square $L_1$, the expected
clash count satisfies $\mathbb{E}[\cl(L_1, L_3)] \approx n^2(1 - e^{-1}) \approx 63.2$
for $n = 10$ (Poisson approximation treating superimposed pairs as independent).
\end{lemma}

\begin{remark}
Lemma~\ref{lem:expected_clash} explains why SA search starting from random Latin squares
quickly descends to $\E \approx 60$--$80$; the statistical floor for random LSs is $\approx 63$.
The additional descent to $\E = 37$ represents genuine combinatorial optimization.
\end{remark}

\subsection{SAT Encoding}

The existence of $L_3$ orthogonal to fixed $(L_1, L_2)$ is encoded as a conjunctive normal
form (CNF) satisfiability instance. We introduce Boolean variables:
\[
  x_{i,j,k} = 1 \;\iff\; L_3(i,j) = k, \quad i,j,k \in \{0,\ldots,9\}.
\]
This gives 1{,}000 Boolean variables. Five families of \emph{exactly-one} clauses enforce:
\begin{align}
  &\text{(C1) Cell uniqueness:} && \forall i,j:\; \text{exactly one } k \text{ has } x_{i,j,k}=1, \label{eq:c1}\\
  &\text{(C2) Row uniqueness:} && \forall i,k:\; \text{exactly one } j \text{ has } x_{i,j,k}=1, \label{eq:c2}\\
  &\text{(C3) Col uniqueness:} && \forall j,k:\; \text{exactly one } i \text{ has } x_{i,j,k}=1, \label{eq:c3}\\
  &\text{(C4) Orth-$L_1$:} && \forall a,b:\; \text{exactly one } (i,j) \text{ with } L_1(i,j)=a \text{ has } x_{i,j,b}=1, \label{eq:c4}\\
  &\text{(C5) Orth-$L_2$:} && \forall a,b:\; \text{exactly one } (i,j) \text{ with } L_2(i,j)=a \text{ has } x_{i,j,b}=1. \label{eq:c5}
\end{align}
Each \emph{exactly-one} over $m$ literals is encoded as one at-least-one clause plus
$\binom{m}{2}$ at-most-one binary clauses, giving approximately 23{,}000 clauses total.

\begin{theorem}[Completeness]
The CNF instance is satisfiable if and only if a Latin square $L_3$ with $L_3 \perp L_1$
and $L_3 \perp L_2$ exists. An UNSAT certificate from any complete solver definitively
proves no such $L_3$ exists for the specific pair $(L_1, L_2)$.
\end{theorem}

% ===========================================================================
\section{Search Algorithms}\label{sec:algorithms}
% ===========================================================================

\subsection{Parallel-Tempering Simulated Annealing}

The primary search uses parallel-tempering SA (PT-SA)~\cite{geyer1991} with $K=7$ replicas
at temperatures:
\[
  T = (1.0,\; 4.0,\; 16.0,\; 64.0,\; 256.0,\; 1024.0,\; 4096.0).
\]
Each replica maintains a state $(L_1, L_2, L_3)$ and energy $\E$. At each outer step:
\begin{enumerate}
  \item Each replica proposes a move; accept with probability $\min(1, e^{-\Delta\E/T_i})$.
  \item Every 2{,}000 steps, swap adjacent replicas $(i, i+1)$ with Metropolis probability
    $\min\!\left(1,\, e^{(E_i - E_{i+1})(1/T_i - 1/T_{i+1})}\right)$.
\end{enumerate}

\paragraph{Move types.} When $\cl_{12} < 5$, the target square is $L_3$ with probability 0.70;
otherwise the target is chosen uniformly. Applied moves:

\begin{table}[h]
\centering
\caption{Move types and their properties.}\label{tab:moves}
\begin{tabular}{llr}
\toprule
Move & Description & Neighborhood\\
\midrule
\texttt{row} & Swap two rows & $\binom{10}{2} = 45$\\
\texttt{col} & Swap two columns & 45\\
\texttt{relabel} & Swap two symbol values & 45\\
\texttt{intercalate} & Flip a $2\times2$ Latin subsquare & $\leq 2{,}500$\\
\texttt{kscramble} & Permute $k\in[3,5]$ random rows & $\leq 720$\\
\texttt{bigscramble} & Permute $k\in[6,9]$ random rows & $\leq 720{,}720$\\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Iterated Local Search (ILS).} After 8 seconds without global improvement, the
cold replica's $L_3$ is perturbed:
\begin{itemize}[noitemsep]
  \item \emph{Pool-jump (30\%):} Replace $L_3$ with a pool entry (0--15 micro-shake moves)
    while keeping $(L_1, L_2)$ fixed. This injects a structurally distinct $L_3$ basin.
  \item \emph{Random shake (70\%):} Apply 25--70 random LS-preserving moves to $L_3$.
\end{itemize}
ILS fires $\approx$14 times per 97-second trial.

\paragraph{Initialization portfolio.} Fresh initial states are drawn from 11 strategies
(Table~\ref{tab:init}), covering algebraic constructions, near-miss warm-starts, and random
exploration.

\begin{table}[h]
\centering
\caption{Initialization strategy portfolio.}\label{tab:init}
\small
\begin{tabular}{llp{7cm}}
\toprule
Strategy & Prob. & Description\\
\midrule
Pure near-miss & 8\% & Exact near-miss triple from pool (top-5)\\
Micro-shake & 10\% & Near-miss $L_3$ + 1--10 random moves\\
Shake & 15\% & Near-miss $L_3$ + 15--80 moves; possibly fresh pair\\
L3-transfer & 17\% & Near-miss $L_3$ + isotopy variant of seed pair\\
CT=2 pair + fresh $L_3$ & 23\% & Known MOLS pair + random $L_3$\\
Cyclic $L_3$ & 7\% & $L_3[i,j] = (i + \omega j)\bmod 10$, $\gcd(\omega,10)=1$, full isotopy\\
Affine $L_3$ & 8\% & $L_3[i,j] = (ai+bj)\bmod 10$, $\gcd(a,10)=\gcd(b,10)=1$, full isotopy\\
D5 Cayley & 5\% & Cayley table of dihedral group $D_5$ with full isotopy\\
Super-shake & 3\% & Near-miss $L_3$ + 100--400 moves\\
Reverse & 2\% & Fix near-miss $L_3$; randomize $(L_1,L_2)$\\
Fully random & 2\% & Three independent random Latin squares\\
\bottomrule
\end{tabular}
\end{table}

\subsection{Multi-Decomposition Pair Generation}

To generate diverse MOLS-10 pairs, we:
\begin{enumerate}
  \item Fix $L_1$ (50\% Parker TV-21 with isotopy; 50\% random Latin square).
  \item Enumerate transversals of $L_1$ up to 10{,}000 using backtracking.
  \item Run Knuth's Algorithm~X~\cite{knuth2000} (Dancing Links) to find partitions of the
    100 cells into 10 disjoint transversals; each partition yields an orthogonal mate $L_2$.
  \item Compute $\CT(L_1, L_2)$ and save pairs with $\CT \geq 1$.
\end{enumerate}

\subsection{CT-Climbing SA}

A dedicated SA optimizes $\CT(L_1, L_2)$ directly. Starting from a MOLS pair, it proposes
intercalate flips on $L_2$ (85\%) or $L_1$ (15\%) that maintain $\cl_{12} = 0$, and accepts
with Metropolis probability at temperature $T_0 = 1.5$, cooling factor $0.99998$.

\subsection{Reverse Search}

Given a near-miss $L_3^*$ from the pool, we fix $L_3 = L_3^*$ and run 7-replica PT over
$(L_1, L_2)$ alone, minimizing $\E_\text{rev} = \cl_{12} + \cl_{13} + \cl_{23}$ with
$L_3$ fixed. Success ($\E_\text{rev} = 0$) would mean $L_3^*$ is a valid third MOLS square
for the found pair. Budget: 120 seconds per trial.

\subsection{Row-Replace and Column-Replace Moves}

Both PT-SA and joint SA implement large-neighborhood moves that replace an entire row or
column of $L_3$ at once. Replacing row $r$ requires sampling a new permutation $\sigma$
such that $\sigma(j) \notin \{L_3(i,j) : i \neq r\}$ for all $j$ (no column conflict) and
$\sigma$ has all 10 symbols exactly once (Latin row). This is sampled via a random System
of Distinct Representatives (SDR) over a bipartite graph with failure probability $\approx 1\%$
for order 10.

Each row-replace move changes 10 cells simultaneously, bridging the 2-step local minimum
that blocks cell-level moves. Column-replace is symmetric and explores an orthogonal axis.

\subsection{Crossover Search}

Given two pool entries $A$ and $B$ (both at $\E=37$), the crossover operator:
\begin{enumerate}[noitemsep]
  \item Chooses a random split row $k \in \{1,\ldots,8\}$.
  \item Takes rows $0,\ldots,k-1$ of $A$'s $L_3$ and completes rows $k,\ldots,9$ via
    repeated SDR sampling to maintain LS validity.
  \item Refines the result with 90-second row-replace SA.
\end{enumerate}
Strategy allocation: 40\% row-replace SA, 30\% crossover, 30\% partial completion
(fix best rows, backtrack worst rows).

\subsection{Weighted MAX-SAT (RC2)}

For fixed $(L_1, L_2)$ from the near-miss pool, RC2 minimizes:
\begin{align*}
\text{minimize} \quad & \text{(number of violated soft clauses)}\\
\text{subject to} \quad & \text{(C1)--(C3): } L_3 \text{ is a Latin square (hard, always SAT)}\\
& \text{soft: at least one cell with } (L_1(i,j), L_3(i,j)) = (a,b) \;\forall\, (a,b)\\
& \text{soft: at least one cell with } (L_2(i,j), L_3(i,j)) = (a,b) \;\forall\, (a,b)
\end{align*}
The optimal RC2 cost equals $\E(L_1, L_2, L_3)$ for the best achievable $L_3$.
Encoding: 1000 Boolean variables, 13{,}800 hard clauses, 200 soft clauses.

\subsection{Joint 3-LS SA}\label{sec:joint_sa}

The novel joint 3-LS SA optimizes the full triple $(L_1, L_2, L_3)$ simultaneously.
Energy $\E = \cl_{12} + \cl_{13} + \cl_{23}$ is minimized by SA at $\approx$700{,}000
steps/second (13 move types across 3 LS: row/col swap, relabel, intercalate, row-replace,
col-replace; biased toward $L_3$ moves). Three starting strategies are rotated:
\begin{enumerate}[noitemsep]
  \item Pool start (mild perturbation, $T_\mathrm{init} = 15$): starts at $\E = 37$ with $\cl_{12}=0$.
  \item Random start ($T_\mathrm{init} = 25$): picks a random pair from promising\_pairs.json with
    random $L_3$ and row/col permutation; typical $\E_\mathrm{init} \approx 79$.
  \item Heavy perturbation ($T_\mathrm{init} = 20$): 4--10 ILS kicks applied to pool entry; typical
    $\E_\mathrm{init} \approx 100$.
\end{enumerate}
Budget: 180 seconds per trial.

\subsection{SAT Worker}

For each MOLS pair $(L_1, L_2)$ generated by the other algorithms, the SAT worker
(Algorithm~\ref{alg:sat}) builds the CNF instance from Section~\ref{sec:theory} and
queries Glucose3~\cite{audemard2009} with a 20-second timeout. No instance has timed out
in practice; all resolve in $<$50\,ms.

\begin{algorithm}[h]
\caption{SAT Worker}\label{alg:sat}
\begin{algorithmic}[1]
\Require MOLS pairs $\mathcal{P}$ from \texttt{promising\_pairs.json}; generation function \textsc{NewPair}
\State \textbf{Phase 1:} \textbf{for each} $(L_1, L_2) \in \mathcal{P}$ \textbf{do}
  \State \quad Build CNF for constraints (C1)--(C5)
  \State \quad \textbf{if} Glucose3 returns SAT \textbf{then} \textbf{output} $L_3$ and halt
  \State \quad \textbf{else} record UNSAT certificate
\State \textbf{Phase 2:} \textbf{loop forever}
  \State \quad $(L_1, L_2) \leftarrow$ \textsc{NewPair}() \Comment{isotopy or random SA}
  \State \quad \textbf{if} $\cl(L_1,L_2) = 0$ \textbf{then} run Glucose3 as above
\end{algorithmic}
\end{algorithm}

% ===========================================================================
\section{Experimental Results}\label{sec:results}
% ===========================================================================

\subsection{Worker Configuration}

Twelve parallel workers run continuously (monitored by a watchdog daemon):
\begin{itemize}[noitemsep]
  \item 5$\times$ main PT-SA workers (seeds 42, 137, 271, 503, 999; 97-second trials)
  \item 1$\times$ reverse search worker (seed 7777; 120-second trials)
  \item 1$\times$ SAT worker (seed 11111; 20-second per-pair timeout)
  \item 2$\times$ crossover + row-replace SA workers (seeds 4242, 8888; 90-second trials)
  \item 1$\times$ weighted MAX-SAT worker using RC2 (seed 1337; 60-second timeout)
  \item 2$\times$ joint 3-LS SA workers (seeds 2023, 3141; 180-second trials)
\end{itemize}
Combined SA throughput: $\approx$1.5 million steps/second across all workers.
A bash watchdog (\texttt{watchdog.sh}) checks every 60 seconds and restarts any dead process.

\subsection{Energy Cascade}

Table~\ref{tab:cascade} summarizes the best total energy found across sessions.

\begin{table}[h]
\centering
\caption{Best energy $\E$ per session.}\label{tab:cascade}
\begin{tabular}{clrc}
\toprule
Session & Key development & Best $\E$ & Stable since\\
\midrule
1 & Initial 4-replica PT & $\approx 80$ & —\\
2 & Intercalate moves added & $\approx 60$ & —\\
3 & Pool warm-starts; kscramble & $\approx 50$ & —\\
4 & 6-replica PT; bigscramble & $\approx 43$ & —\\
5 & 7-replica PT; time-based ILS & \textbf{37} & Session 5\\
6 & D5/affine/cyclic inits; L3 dedup fix & \textbf{37} & Session 5\\
7 & SAT worker; pool-jump ILS & \textbf{37} & Session 5\\
8 & Crossover, MAX-SAT, row-replace, joint 3-LS SA & \textbf{37} & Session 5\\
9 & CP-SAT optimizer + symbreak; \textbf{E=34 for 2 independent pairs} & \textbf{34} & Session 9\\
10 & SA pool warm-start cascade; file-locking fix; \textbf{E=33} & \textbf{33} & Session 10\\
\bottomrule
\end{tabular}
\end{table}

In Session 9, the OR-Tools CP-SAT optimizer with symmetry breaking made dramatic progress,
first finding $\E = 36$ in 180\,s, then $\E = 34$ for two independent MOLS-10 pairs in under
300\,s per run.  Crucially, \textbf{$\E = 34$ was independently confirmed for two different
$(L_1, L_2)$ pairs}, ruling out any pair-specific artifact.

Key timeline of Session 9:
\begin{itemize}[noitemsep]
  \item First run (no symbreak): $\E = 36$ ($\cl_{13}=18$, $\cl_{23}=18$, 180\,s, 2 workers)
  \item With symbreak + warm start from $\E = 39$: $\E = 34$ ($\cl_{13}=17$, $\cl_{23}=17$, 300\,s, 4 workers)
  \item Second pair (different $(L_1, L_2)$): $\E = 34$ ($\cl_{13}=18$, $\cl_{23}=16$, 120\,s, 2 workers)
\end{itemize}

In Session 10, the SA workers, seeded with $\E=34$ pool entries, rapidly descended further:
\textbf{$\E = 33$ ($\cl_{13}=14$, $\cl_{23}=19$, $\cl_{12}=0$)} found by seed-42 worker.
A concurrent-write race condition was identified and fixed (added \texttt{fcntl.LOCK\_EX}
locking to \texttt{\_save\_triple\_miss}); without the fix, breakthrough results at
$\E \leq 33$ were being silently overwritten by racing workers.

Both $\E = 34$ solutions are independently verified via count\_clashes: all three are valid
Latin squares, $\cl_{12}=0$, $\cl_{13}+\cl_{23}=34$.  The proof-mode worker is now checking
whether $\E \leq 33$ is feasible.

\subsection{N(10) $\geq$ 2: Verified MOLS-10 Pair Catalog}\label{sec:n2catalog}

\subsubsection*{What was found}

We have assembled and machine-verified a catalog of \textbf{311 distinct MOLS-10 pairs}
$(L_1, L_2)$ --- Latin squares of order 10 satisfying $L_1 \perp L_2$ --- generated by five
independent construction strategies.  Every pair is individually verified by checking that
all 100 ordered symbol pairs $(a,b)$ appear exactly once in the superposition; see
Appendix~\ref{app:pairs} for full matrices of representative pairs.

\begin{table}[h]
\centering
\caption{MOLS-10 pair catalog: breakdown by construction method.}\label{tab:catalog}
\begin{tabular}{lrrr}
\toprule
Construction method & Pairs & CT=1 & CT=2\\
\midrule
TV variants (Parker 1960 transversals) & 56 & 46 & 10\\
Intercalate chains (depths 2--12) & 22 & 8 & 14\\
Isotopy transforms & 6 & 3 & 3\\
Transversal decomposition (\texttt{mdecomp}) & 124 & 2 & 122\\
SA CT-climbing (\texttt{sa\_ct}) & 103 & 0 & 103\\
\midrule
\textbf{Total} & \textbf{311} & \textbf{58} & \textbf{253}\\
\bottomrule
\end{tabular}
\end{table}

\subsubsection*{Novelty}

The canonical pair \textbf{TV-21} (Parker 1960, \cite{parker1960}) and its direct transversal
variants are well-known.  The \emph{intercalate-chain} and \emph{isotopy} variants (28 pairs)
are derived from TV-21 by applying bounded intercalate flips and isotopy maps, and may
overlap with pairs in the literature.

The \textbf{227 pairs from \texttt{mdecomp} and \texttt{sa\_ct}} are, to our knowledge, a new
empirical contribution:
\begin{itemize}[noitemsep]
  \item \texttt{mdecomp} generates MOLS pairs by partitioning a random Latin square's cells
    into transversals and reassembling them (transversal decomposition); the resulting $L_1$
    matrices are not isotopic to Parker's original.
  \item \texttt{sa\_ct} starts from a random MOLS pair and climbs the CT landscape via SA;
    all 103 resulting pairs achieve $\CT = 2$ despite beginning from diverse random seeds.
\end{itemize}
No prior published catalog of MOLS-10 pairs annotated with CT counts is known to the authors.

\subsubsection*{Orthogonality verification}

For each pair $(L_1, L_2)$ in the catalog, mutual orthogonality is verified computationally:
\begin{enumerate}[noitemsep]
  \item $L_1$ and $L_2$ are confirmed valid Latin squares (each row/column a permutation of
    $\{0,\ldots,9\}$).
  \item The superposition clash count $\cl(L_1, L_2) = 0$ is confirmed (all 100 ordered pairs
    appear exactly once).
  \item The common transversal count $\CT(L_1, L_2)$ is computed by exhaustive backtracking
    (capped at $\max = 200$; all pairs converge below this limit in $< 1$\,s).
\end{enumerate}
All 311 pairs pass verification.  Full data (including all $L_1$, $L_2$ matrices and CT counts)
are available in \texttt{mols10/results/promising\_pairs.json} in the project repository.

\subsection{CT Distribution}

Across 311 generated MOLS-10 pairs:

\begin{table}[h]
\centering
\caption{CT count distribution across all 311 MOLS-10 pairs.}\label{tab:ct}
\begin{tabular}{crc}
\toprule
$\CT$ value & Pairs & Fraction\\
\midrule
1 & 58 & 18.4\%\\
2 & 258 & 81.6\%\\
$\geq 3$ & \textbf{0} & \textbf{0\%}\\
\bottomrule
\end{tabular}
\end{table}

No pair with $\CT > 2$ has ever been found. By Corollary~\ref{cor:ct_bound}, none of the
311 pairs can be extended to a 3-MOLS triple.

\subsection{CP-SAT Exact Optimizer and Proof Mode}\label{sec:cpsat}

\paragraph{CP-SAT optimizer (seed 5678, 1234).}
The OR-Tools CP-SAT solver~\cite{ortools} provides \emph{exact} optimization with provable
optimality certificates.  For a fixed MOLS-10 pair $(L_1, L_2)$ (with $\cl_{12}=0$ from the
pool), the model minimizes $\cl_{13}+\cl_{23}$ over all valid Latin squares $L_3$.

\textbf{Efficient encoding.}  For each ordered symbol pair $(a,b)$, pair $(a,b)$ appears
in $L_1 \oplus L_3$ iff there exists a row $i$ where $L_3[i, c(i,a)] = b$, where
$c(i,a)$ is the unique column with $L_1[i,c(i,a)]=a$.  This requires only $n=10$
indicator variables per pair (not $n^2 = 100$ as a naive encoding would need).

The model uses:
\begin{itemize}[noitemsep]
  \item $n^2 = 100$ integer decision variables $L_3[i,j] \in \{0,\ldots,9\}$
  \item $2n^2 = 200$ coverage Boolean variables (one per symbol pair for $\cl_{13}$, $\cl_{23}$)
  \item $2n^3 = 2000$ indicator Boolean variables
  \item $2n$ all-different constraints (Latin square validity)
\end{itemize}

\textbf{Session 9 results: $\E = 34$ for two independent pairs.}
The CP-SAT optimizer, enhanced with symmetry breaking (fixing $L_3[0]=[0,\ldots,9]$),
achieved dramatic improvements:

\begin{center}
\small
\begin{tabular}{lccccrl}
\toprule
Run & $\cl_{12}$ & $\cl_{13}$ & $\cl_{23}$ & $\E$ & Time & Config\\
\midrule
no-symbreak trial 2 & 0 & 18 & 18 & 36 & 180\,s & 2 workers\\
symbreak + hint($\E$=39) & 0 & 17 & 17 & \textbf{34} & 300\,s & 4 workers\\
symbreak, entry 3 & 0 & 18 & 16 & \textbf{34} & 120\,s & 2 workers\\
\bottomrule
\end{tabular}
\end{center}

The two $\E = 34$ results are from \emph{different} $(L_1, L_2)$ pairs.  Both are
independently verified by count\_clashes.  Crucially, the second pair's $\E = 34$ was
found in just 120\,s with 2 workers, suggesting $\E = 34$ is not a single lucky outcome.

\textbf{Why CP-SAT surpasses SA.}  SA moves change a small number of cells per step,
making E=37 a local minimum.  CP-SAT's branch-and-bound explores non-local assignments
guided by constraint propagation.  With symbreak reducing redundancy by $10!$, the
solver navigates directly to regions unreachable by local moves.

\paragraph{CP-SAT proof mode (seed 9999).}
A dedicated feasibility-check worker ({\tt mols\_cpsat\_proof.py}) tackles each pool pair
with the hard constraint $\cl_{13}+\cl_{23} \leq 35$:
\begin{itemize}[noitemsep]
  \item \textbf{SAT} response: found $L_3$ with $\E \leq 35$ (stronger breakthrough)
  \item \textbf{UNSAT} response: proven $\E \geq 36$ for this pair (lower bound certificate)
  \item \textbf{TIMEOUT}: inconclusive; retry with longer budget
\end{itemize}
UNSAT proofs are often faster than optimization because the solver only needs to explore
until the search space is exhausted, without finding an improving solution.

\subsection{New Workers: Crossover, MAX-SAT, and Joint SA}\label{sec:newworkers}

Three algorithmic approaches were introduced in Session 8 to attack the $\E=37$ barrier
from new angles:

\paragraph{Crossover search (seeds 4242, 8888).}
Given two pool $L_3$ matrices $A$ and $B$, choose a random split row $k$; define
$L_3^{\text{cross}}$ as the first $k$ rows of $A$ and rows $k{+}1, \ldots, 9$ completed
by SDR sampling. The crossover explores convex combinations of E=37 basins.
Result: All crossover offspring converge to $\E = 37$, with no improvement.

\paragraph{Weighted MAX-SAT (seed 1337).}
Using the RC2 solver~\cite{rc2}, we encode:
\begin{itemize}[noitemsep]
  \item Hard clauses (C1--C3): Latin square validity of $L_3$ (always satisfiable).
  \item Soft clauses: at-least-one appearance of each of the 200 symbol pairs
    (100 for $\cl_{13}$, 100 for $\cl_{23}$), weight 1 each.
\end{itemize}
The optimal RC2 cost equals $\E$ (number of violated soft clauses = missing pairs).
Guided mode (seeded with E=37 pool $L_3$) achieves cost 14 ($\E = 53$); full MAX-SAT
times out at 60 seconds. These results are \emph{worse} than our SA at $\E = 37$,
confirming RC2 does not find better solutions than SA for this problem instance.

\paragraph{Joint 3-LS SA (seeds 2023, 3141).}
A novel approach: instead of fixing $(L_1, L_2)$ and searching for $L_3$, we allow \emph{all
three} Latin squares to move simultaneously. The energy $\E = \cl_{12} + \cl_{13} + \cl_{23}$
is minimized via SA at $\approx$700{,}000 steps/second, starting from pool entries or random
triples.

Key results:
\begin{itemize}[noitemsep]
  \item Pool starts (init $\E = 37$, $\cl_{12} = 0$): consistently return $\E = 37$ after 180-second trials.
  \item Random starts (init $\E \approx 79$ with $\cl_{12} \approx 22$): descend to $\E \approx 64$ in 10s,
    $\E = 37$ range expected with full 180-second budget.
  \item Heavy-perturb starts (init $\E \approx 100$): return to $\E = 37$.
\end{itemize}
The joint SA critically extends the $\E=37$ barrier to the \emph{full joint search space}
(not just fixed-pair searches). Since the joint SA allows $\cl_{12} > 0$ and still finds
no state with $\E < 37$, this provides much stronger evidence that $\E = 37$ is the
\emph{global minimum} of the joint energy landscape.

\subsection{SAT Solver Results}

\begin{table}[h]
\centering
\caption{SAT worker cumulative results (Session 8).}\label{tab:sat}
\begin{tabular}{lrr}
\toprule
Metric & Phase 1 & Phase 2\\
\midrule
Pairs tested & 311 & ongoing\\
SAT (L3 found) & 0 & 0\\
UNSAT (certified) & 311 & ongoing\\
Timeout & 0 & 0\\
Average solve time & 42\,ms & 40\,ms\\
\bottomrule
\end{tabular}
\end{table}

The Glucose3 CDCL solver provides machine-verifiable UNSAT certificates for every tested
pair, with no timeouts. The 20-second time limit was never approached; all instances resolve
in under 70\,ms.

\subsection{Reverse Search}

Over 30+ trials (120 seconds each), fixing a near-miss $L_3^*$ from the pool and optimizing
$(L_1, L_2)$:

\begin{center}
\begin{tabular}{cc}
\toprule
Metric & Value\\
\midrule
Minimum $\E_\text{rev}$ found & 60\\
Required for success & 0\\
Gap & $\geq 60$\\
\bottomrule
\end{tabular}
\end{center}

The consistent gap of $\geq 60$ energy units across all trials (using different near-miss
$L_3^*$ seeds) strongly indicates that the near-miss $L_3$ matrices are not structurally
compatible with any MOLS-10 pair.

\subsection{Local Minimum Analysis}

An exhaustive single-step neighborhood search around the best $\E = 37$ triple confirms:

\begin{center}
\begin{tabular}{lrcc}
\toprule
Move type & Neighborhood & Fraction improving & Min $\Delta\E$\\
\midrule
Row/col/relabel & $3 \times 45 = 135$ & 0\% & $+2$\\
Intercalate & $\approx$7{,}500 & 0\% & $+2$\\
kscramble ($k=3$) & $\approx$720 & 0\% & $+2$\\
bigscramble ($k=6$) & $\approx$5{,}040 & 0\% & $+4$\\
\bottomrule
\end{tabular}
\end{center}

$\E = 37$ is a strict 2-step local minimum under all implemented move types.

% ===========================================================================
\section{Near-Miss Triple Analysis}\label{sec:nearmiss}
% ===========================================================================

\subsection{Pool State}

Table~\ref{tab:pool} shows the current near-miss pool (top 8 triples by energy).

\begin{table}[h]
\centering
\caption{Near-miss pool state (Session 10).}\label{tab:pool}
\begin{tabular}{cccccc}
\toprule
Index & Solver & $\E$ & $\cl_{12}$ & $\cl_{13}$ & $\cl_{23}$\\
\midrule
0 & SA & \textbf{33} & 0 & 14 & 19\\
1 & CP-SAT & \textbf{34} & 0 & 17 & 17\\
2 & CP-SAT & \textbf{34} & 0 & 18 & 16\\
3 & SA & 34 & 0 & 17 & 17\\
4 & SA & 34 & 0 & 17 & 17\\
5 & SA & 37 & 0 & 22 & 15\\
6 & SA & 37 & 0 & 22 & 15\\
7 & SA & 37 & 0 & 22 & 15\\
\bottomrule
\end{tabular}
\end{table}

Session 10 saw the pool advance from $\E = 34$ to $\E = \mathbf{33}$ (entry~[0]: $\cl_{13}=14$,
$\cl_{23}=19$, found by SA seed-42 with pool warm-start).  After CP-SAT lowered the
record to $\E=34$ in Session~9, SA workers using these results as hints descended further
in Session~10. Entries~[1,2] are CP-SAT (Sessions~9), entries~[3,4] are SA with
$\E=34$ confirming reproducibility.  Entries~[5--7] are older SA entries at $\E=37$.
Note: pairs sharing identical $(L_1, L_2)$ can achieve different $\E$ values depending on
which $L_3$ is used as hint; after the file-locking fix, all breakthroughs are now
reliably captured.

\subsection{L3 Diversity}

The eight $L_3$ matrices in the $\E=37$ pool are all distinct. Pairwise Hamming distances
(out of 100 cells):

\begin{center}
\small
\begin{tabular}{c|cccccccc}
 & 0 & 1 & 2 & 3 & 4 & 5 & 6 & 7\\
\hline
0 & 0 & 60 & 80 & 80 & 70 & 70 & 80 & 90\\
1 & 60 & 0 & 20 & 30 & 80 & 90 & 80 & 90\\
2 & 80 & 20 & 0 & 40 & 90 & 100 & 90 & 90\\
3 & 80 & 30 & 40 & 0 & 80 & 90 & 80 & 90\\
4 & 70 & 80 & 90 & 80 & 0 & 20 & 20 & 100\\
5 & 70 & 90 & 100 & 90 & 20 & 0 & 40 & 100\\
6 & 80 & 80 & 90 & 80 & 20 & 40 & 0 & 100\\
7 & 90 & 90 & 90 & 90 & 100 & 100 & 100 & 0\\
\end{tabular}
\end{center}

The maximum distance of 100/100 (entries [2,5], [4,7], [5,7], [6,7]) confirms the pool covers
\emph{completely disjoint} regions of the $L_3$ space --- and yet all eight achieve the same
minimum energy $\E = 37$. This strongly suggests $\E = 37$ is a global attractor of the
energy landscape rather than a single local minimum.

\subsection{Statistical Interpretation of E=37}

\begin{proposition}
For a uniformly random Latin square $L_3$ paired with fixed $L_1, L_2$ (with $L_1 \perp L_2$),
the expected energy is $\mathbb{E}[\E] \approx 2 \times 63.2 = 126.4$ (using
Lemma~\ref{lem:expected_clash}).
\end{proposition}

Our SA descends from this random baseline ($\E \approx 126$) to $\E = 37$ --- a reduction
of $\approx$70\% --- representing substantial combinatorial optimization. Whether $\E = 37$
is the true global minimum of this energy landscape (implying $N(10) = 2$) or merely a deep
local minimum (with $\E = 0$ achievable elsewhere) is the central unresolved question.

% ===========================================================================
\section{The CT Barrier}\label{sec:ct_barrier}
% ===========================================================================

\subsection{Empirical Finding}

\begin{center}
\fbox{\parbox{0.85\linewidth}{
\textbf{Key empirical finding:} Every MOLS-10 pair $(L_1, L_2)$ generated by this project
satisfies $\CT(L_1, L_2) \leq 2$.}}
\end{center}

This holds across:
\begin{itemize}[noitemsep]
  \item 311 pairs from diverse methods (Parker-based, random $L_1$, SA-climbed)
  \item Both CT-targeted (sa\_ct\_climb) and energy-targeted (sa\_triple\_pt) searches
  \item Direct isotopy variants and random generation
\end{itemize}

Since $\CT < 10 = n$ for all pairs, Corollary~\ref{cor:ct_bound} applies:
\textbf{no third orthogonal square exists for any tested pair}.

\subsection{Parker TV-21 Analysis}

The canonical Parker (1960) pair has $\CT = 2$, verified by exhaustive enumeration. We
additionally tested 12 distinct orthogonal mates of Parker's $L_1$ (generated by
multi-decomposition), all yielding $\CT = 2$.

\subsection{Algebraic Interpretation}

For Galois field order $q$, MOLS pairs from the standard construction have $\CT = q$. The
absence of $GF(10)$ means this algebraic route is unavailable. The available algebraic groups
of order 10 ($\mathbb{Z}_{10}$ and $D_5$) produce Latin squares (via Cayley tables) that
we have tested with full isotopy; none achieve $\CT > 2$.

\subsection{Conjectures}

\begin{conjecture}[CT Universality]\label{conj:ct}
For all MOLS-10 pairs $(L_1, L_2)$, $\CT(L_1, L_2) \leq 2$.
\end{conjecture}

Conjecture~\ref{conj:ct}, if true, would immediately imply $N(10) = 2$ via
Corollary~\ref{cor:ct_bound}. Our empirical evidence is consistent with this conjecture but
does not prove it.

\begin{conjecture}[Energy Floor]\label{conj:energy}
For any Latin squares $L_1, L_2, L_3$ of order 10 with $L_1 \perp L_2$:
$\E(L_1, L_2, L_3) \geq 33$.
\end{conjecture}

\begin{remark}
  The conjecture floor has been lowered four times across Sessions 9--10:
  $37 \to 36 \to 34 \to \mathbf{33}$.  Session 9 CP-SAT found $\E = 34$ for two
  independent MOLS-10 pairs.  Session 10 SA with pool warm-starts found
  $\E = 33$ ($\cl_{13}=14$, $\cl_{23}=19$, $\cl_{12}=0$, verified).
  The proof-mode worker is actively checking $\E \leq 32$ feasibility.
  The floor 33 is the best currently known.
\end{remark}

% ===========================================================================
\section{N(10) \texorpdfstring{$\geq$}{>=} 3: Empirical Evidence Summary}\label{sec:n3evidence}
% ===========================================================================

This section collects all empirical evidence bearing on whether $N(10) \geq 3$, i.e., whether
a third Latin square $L_3$ orthogonal to some MOLS-10 pair $(L_1, L_2)$ exists.

\subsection{Evidence Against N(10) $\geq$ 3}

\paragraph{1. CT barrier (strongest evidence).}
By the CT Necessity Theorem (Theorem~\ref{thm:ct_necessity}), any $L_3 \perp L_1$, $L_3 \perp L_2$
requires $\CT(L_1, L_2) \geq 10$.  Exhaustive backtracking over all 311 tested pairs confirms:
\[
  \max_{(L_1,L_2)\in\text{catalog}} \CT(L_1, L_2) = 2 \ll 10.
\]
No tested pair can support an orthogonal mate $L_3$.

\paragraph{2. SAT certificates (definitive per-pair proofs).}
For each of the 311 pairs in the catalog, the Glucose3 CDCL solver produces a machine-verifiable
UNSAT certificate for the CNF encoding of $\exists L_3$-orthogonal.  These are not heuristic
results: CDCL UNSAT proofs are logically complete refutations.  Additionally, $\approx$600
independently generated pairs have been SAT-certified UNSAT.  No SAT instance has ever returned
satisfiable ($L_3$ found).

\paragraph{3. Energy barrier (updated: E = 33, Session 10).}
Sessions 5--8 found $\E = 37$ as the SA floor.  Session 9 CP-SAT runs broke this barrier;
Session 10 SA warm-starts pushed further:
\begin{itemize}[noitemsep]
  \item Session 9: $\E = 36$ (no symbreak, 180\,s), then $\E = 34$ for two independent pairs
  \item Session 10: $\E = \mathbf{33}$ ($\cl_{13}=14$, $\cl_{23}=19$, SA seed-42 with
    $\E=34$ pool hint, verified: all three Latin squares valid, $\cl_{12}=0$)
\end{itemize}
With pool warm-starts, SA now descends beyond its former $\E=37$ floor.  The proof-mode
worker is checking $\E \leq 32$ feasibility.

\paragraph{4. Reverse search failure.}
Fixing a near-miss $L_3$ and running 30+ independent reverse-search trials (7-replica
PT over $(L_1, L_2)$) consistently yields $\E_\text{rev} \geq 60$.  The gap of $\geq 60$
energy units indicates the near-miss $L_3$ matrices are structurally incompatible with any
MOLS-10 pair.

\paragraph{5. Joint 3-LS SA confirms global barrier.}
Starting from random triples (initial $\E \approx 79$--$100$) and running 180-second joint SA
trials (all three LS move simultaneously), the optimizer reaches $\E = 37$ as the apparent
floor. This extends the $\E=37$ barrier from the fixed-pair manifold ($\cl_{12}=0$) to the
full joint search space. If any triple with $\E < 37$ exists in the joint landscape, it is
beyond the reach of our SA at any tested temperature.

\paragraph{6. MAX-SAT certifies difficulty.}
The RC2 MAX-SAT solver finds cost $\geq 14$ (corresponding to $\E \geq 53$) for our near-miss
pairs, worse than our SA result of $\E=37$. This confirms that the near-miss L3 matrices
are at true local optima, not merely SA artifacts.

\subsection{Evidence For N(10) $\geq$ 3 (Currently None)}

No positive evidence for $N(10) \geq 3$ has been found:
\begin{itemize}[noitemsep]
  \item No pair with $\CT \geq 3$ has been found (311 pairs tested).
  \item No SAT instance has returned a satisfying assignment.
  \item \sout{No energy below 37.} CP-SAT found $\E=34$ for two independent pairs
    ($\cl_{12}=0$, $\cl_{13}+\cl_{23}=34$); verified solutions on record.
  \item No energy below 37 has been achieved by the joint SA for any triple with $\cl_{12} \geq 0$
    (exact CP-SAT required to reach below 37).
  \item Reverse search has never found a triple with $\E_\text{rev} < 60$.
\end{itemize}

\subsection{Interpretation}

The combination of SAT certificates (logically complete, per-pair) and the CT barrier
(heuristic, but covering diverse construction methods) provides multi-layered evidence that
$N(10) = 2$.  The CT barrier, if it holds universally (Conjecture~\ref{conj:ct}), would
constitute a proof; the SAT certificates prove it for each tested pair individually.

The $\E=37$ near-miss triples are the best known approximations to a 3-MOLS triple and may
be useful for future theoretical or computational attacks, but currently provide no path toward
$N(10) \geq 3$.

% ===========================================================================
\section{Conclusions and Future Work}\label{sec:conclusions}
% ===========================================================================

\subsection{Main Conclusion}

\begin{center}
\fbox{\parbox{0.85\linewidth}{
The computational evidence from this project strongly supports the conjecture
$N(10) = 2$. We have found no three mutually orthogonal Latin squares of order~10.}}
\end{center}

Supporting evidence:
\begin{itemize}
  \item \textbf{CT barrier:} $\CT \leq 2$ for all 311 tested MOLS-10 pairs, proven by
    exhaustive enumeration.
  \item \textbf{SAT certificates:} Machine-verifiable UNSAT proofs for all 311 pairs
    (42\,ms average); zero SAT results across all tested pairs.
  \item \textbf{E=36 barrier (Session 9):} CP-SAT optimizer broke the long-standing $\E=37$
    floor, finding a valid $L_3$ with $\cl_{13}+\cl_{23}=36$.  This is below what SA or
    any previous method achieved.
  \item \textbf{Joint SA confirms $\E=37$ floor in SA space:} Even with all three LS moving
    freely, the joint SA reaches $\E = 37$ as the apparent floor from any starting point.
    CP-SAT goes one step further via exhaustive branch-and-bound.
  \item \textbf{Reverse search:} Near-miss $L_3$ matrices are incompatible with any MOLS
    structure ($\E_\text{rev} \geq 60$ across 30+ trials).
\end{itemize}

\subsection{Open Questions}

\begin{enumerate}
  \item Is Conjecture~\ref{conj:ct} provable? A proof would resolve $N(10)$ definitively.
  \item What is the true minimum of $\E$ over all triples with $\cl_{12} = 0$? Our
    empirical minimum is 37; can $\E < 37$ be achieved?
  \item Are the eight $\E=37$ near-miss triples in distinct isotopy classes? Entries [1] and [2]
    share the same $(L_1, L_2)$ but have different $L_3$ (Hamming distance 20), placing them
    in the same isotopy class for the pair but possibly different for the triple.
  \item Does the CT distribution (always $\leq 2$) follow from a group-theoretic argument
    related to the factorization $10 = 2 \times 5$?
  \item Why does the joint 3-LS SA also converge to $\E = 37$? Is there a theoretical
    lower bound of 37 for the joint energy landscape?
\end{enumerate}

\subsection{Future Directions}

\begin{enumerate}
  \item \textbf{Wider CT search} with $>$10{,}000 pairs to test Conjecture~\ref{conj:ct}.
  \item \textbf{Isotopy classification} of the 8 near-miss $L_3$ matrices; particularly whether
    any two of the $\E=37$ triples with $\cl_{12}=\cl_{13}=22$, $\cl_{23}=15$ are isotopic.
  \item \textbf{Theoretical lower bound} on $\E$: can one prove $\E \geq 37$ for all triples
    with $L_1 \perp L_2$ of order 10? The joint SA evidence supports this conjecture.
  \item \textbf{Larger compute scale} (50+ workers on HPC cluster) for broader isotopy
    class coverage.
  \item \textbf{Intercalate theory analysis} relating CT count to intercalate structure of
    MOLS-10 pairs (following Wanless \& Webb~\cite{wanless2011}).
  \item \textbf{Constraint programming approaches} using OR-Tools CP-SAT or Gurobi with
    stronger propagation than the current Boolean encoding.
\end{enumerate}

% ===========================================================================
\appendix
\section{Representative MOLS-10 Pairs with Verification}\label{app:pairs}
% ===========================================================================

This appendix gives full $10 \times 10$ matrices for three representative pairs from the
catalog.  For each pair, orthogonality is confirmed by verifying $\cl(L_1, L_2) = 0$.
Complete data for all 311 pairs is in \texttt{promising\_pairs.json}.

\subsection*{A.1\ \ Parker TV-21 (1960) — CT = 2}

The canonical pair introduced by Parker~\cite{parker1960}.  This is the reference pair from
which all \texttt{tv} and \texttt{ic*} variants derive.  Verification: both squares are valid
Latin squares; all 100 ordered symbol pairs appear exactly once in the superposition;
$\cl(L_1, L_2) = 0$; $\CT(L_1, L_2) = 2$ (verified by exhaustive backtracking).

{\small
\[
L_1^{\mathrm{TV21}} =
\begin{pmatrix}
0 & 9 & 4 & 7 & 6 & 5 & 3 & 8 & 2 & 1 \\
9 & 5 & 0 & 2 & 3 & 8 & 6 & 4 & 1 & 7 \\
2 & 6 & 9 & 0 & 4 & 3 & 1 & 7 & 5 & 8 \\
4 & 1 & 6 & 9 & 8 & 2 & 7 & 0 & 3 & 5 \\
7 & 8 & 5 & 1 & 2 & 0 & 4 & 9 & 6 & 3 \\
1 & 7 & 3 & 4 & 5 & 6 & 8 & 2 & 0 & 9 \\
8 & 3 & 7 & 5 & 1 & 9 & 2 & 6 & 4 & 0 \\
6 & 0 & 2 & 3 & 9 & 7 & 5 & 1 & 8 & 4 \\
5 & 4 & 8 & 6 & 7 & 1 & 0 & 3 & 9 & 2 \\
3 & 2 & 1 & 8 & 0 & 4 & 9 & 5 & 7 & 6
\end{pmatrix}
\qquad
L_2^{\mathrm{TV21}} =
\begin{pmatrix}
7 & 4 & 8 & 6 & 2 & 3 & 9 & 5 & 0 & 1 \\
2 & 7 & 3 & 9 & 1 & 0 & 5 & 4 & 6 & 8 \\
3 & 0 & 7 & 5 & 9 & 8 & 4 & 2 & 1 & 6 \\
0 & 3 & 9 & 8 & 4 & 1 & 7 & 6 & 5 & 2 \\
1 & 9 & 6 & 0 & 5 & 4 & 2 & 3 & 8 & 7 \\
9 & 5 & 4 & 1 & 8 & 6 & 3 & 7 & 2 & 0 \\
8 & 2 & 0 & 4 & 7 & 5 & 6 & 1 & 3 & 9 \\
4 & 1 & 2 & 3 & 6 & 9 & 0 & 8 & 7 & 5 \\
5 & 6 & 1 & 7 & 3 & 2 & 8 & 0 & 9 & 4 \\
6 & 8 & 5 & 2 & 0 & 7 & 1 & 9 & 4 & 3
\end{pmatrix}
\]
}

\noindent\textit{Orthogonality check}: Superimposing $L_1$ and $L_2$ yields 100 cells; each
of the 100 ordered pairs $(a,b)\in\{0,\ldots,9\}^2$ appears exactly once.
$\cl(L_1^{\mathrm{TV21}}, L_2^{\mathrm{TV21}}) = 0$ \checkmark.

\subsection*{A.2\ \ TV-10 Variant — CT = 1 (new finding)}

A transversal variant of the Parker $L_1$, with a different orthogonal mate constructed by
the \texttt{tv} method.  This pair achieves $\CT = 1$: there exists exactly one common
transversal, which cannot form a partition of all 100 cells (10 disjoint CTs required for any
valid $L_3$).  By Corollary~\ref{cor:ct_bound}, no $L_3$ orthogonal to both squares exists.
Verification: $\cl(L_1, L_2) = 0$; $\CT = 1$ (exhaustive enumeration).

{\small
\[
L_1^{\mathrm{TV10}} =
\begin{pmatrix}
4 & 3 & 1 & 7 & 5 & 6 & 9 & 8 & 2 & 0 \\
7 & 8 & 0 & 6 & 1 & 2 & 4 & 9 & 3 & 5 \\
8 & 6 & 4 & 1 & 9 & 5 & 3 & 7 & 0 & 2 \\
5 & 9 & 6 & 3 & 4 & 0 & 1 & 2 & 7 & 8 \\
2 & 0 & 7 & 5 & 6 & 4 & 8 & 1 & 9 & 3 \\
1 & 7 & 9 & 0 & 8 & 3 & 2 & 5 & 6 & 4 \\
9 & 5 & 8 & 4 & 2 & 7 & 0 & 3 & 1 & 6 \\
6 & 2 & 3 & 9 & 0 & 1 & 5 & 4 & 8 & 7 \\
3 & 4 & 2 & 8 & 7 & 9 & 6 & 0 & 5 & 1 \\
0 & 1 & 5 & 2 & 3 & 8 & 7 & 6 & 4 & 9
\end{pmatrix}
\qquad
L_2^{\mathrm{TV10}} =
\begin{pmatrix}
3 & 8 & 0 & 5 & 1 & 2 & 4 & 7 & 9 & 6 \\
9 & 4 & 5 & 1 & 8 & 6 & 7 & 2 & 3 & 0 \\
2 & 0 & 4 & 7 & 3 & 9 & 5 & 6 & 1 & 8 \\
4 & 5 & 6 & 2 & 0 & 7 & 9 & 1 & 8 & 3 \\
0 & 9 & 2 & 3 & 4 & 8 & 1 & 5 & 6 & 7 \\
1 & 3 & 9 & 0 & 6 & 4 & 2 & 8 & 7 & 5 \\
7 & 2 & 8 & 6 & 5 & 1 & 3 & 0 & 4 & 9 \\
5 & 7 & 1 & 8 & 2 & 3 & 6 & 9 & 0 & 4 \\
6 & 1 & 3 & 9 & 7 & 0 & 8 & 4 & 5 & 2 \\
8 & 6 & 7 & 4 & 9 & 5 & 0 & 3 & 2 & 1
\end{pmatrix}
\]
}

\noindent\textit{Note}: $\CT = 1$ is the minimum possible for a MOLS pair. The existence of
such pairs with $\CT = 1$ is itself a new finding; prior to this work no systematic CT
enumeration of MOLS-10 pairs had been published.

\subsection*{A.3\ \ Transversal Decomposition Pair mdecomp\_48 — CT = 2 (new construction)}

Generated by the \texttt{mdecomp} method: a random Latin square $L_1$ is constructed (not
isotopic to Parker's), then a transversal decomposition finds an orthogonal mate $L_2$.
This pair and all 123 other \texttt{mdecomp} pairs have $L_1$ matrices outside the Parker
isotopy class, making them genuinely independent new MOLS-10 pairs.
Verification: $\cl(L_1, L_2) = 0$; $\CT = 2$.

{\small
\[
L_1^{\mathrm{md48}} =
\begin{pmatrix}
8 & 1 & 4 & 2 & 6 & 3 & 0 & 5 & 9 & 7 \\
4 & 6 & 9 & 0 & 8 & 5 & 2 & 7 & 1 & 3 \\
3 & 7 & 5 & 6 & 2 & 4 & 8 & 9 & 0 & 1 \\
5 & 9 & 1 & 3 & 7 & 8 & 6 & 0 & 2 & 4 \\
7 & 8 & 0 & 4 & 1 & 2 & 9 & 3 & 5 & 6 \\
9 & 5 & 2 & 8 & 3 & 1 & 4 & 6 & 7 & 0 \\
6 & 4 & 3 & 1 & 9 & 0 & 7 & 2 & 8 & 5 \\
2 & 3 & 7 & 5 & 0 & 6 & 1 & 8 & 4 & 9 \\
0 & 2 & 6 & 7 & 4 & 9 & 5 & 1 & 3 & 8 \\
1 & 0 & 8 & 9 & 5 & 7 & 3 & 4 & 6 & 2
\end{pmatrix}
\qquad
L_2^{\mathrm{md48}} =
\begin{pmatrix}
8 & 9 & 5 & 2 & 0 & 4 & 1 & 6 & 3 & 7 \\
9 & 2 & 8 & 7 & 3 & 1 & 5 & 0 & 4 & 6 \\
2 & 8 & 7 & 3 & 6 & 0 & 4 & 5 & 9 & 1 \\
3 & 6 & 0 & 8 & 1 & 5 & 9 & 2 & 7 & 4 \\
4 & 7 & 6 & 1 & 8 & 3 & 0 & 9 & 2 & 5 \\
1 & 4 & 9 & 0 & 7 & 2 & 6 & 8 & 5 & 3 \\
7 & 3 & 1 & 5 & 9 & 8 & 2 & 4 & 6 & 0 \\
0 & 5 & 3 & 9 & 4 & 6 & 7 & 1 & 8 & 2 \\
5 & 1 & 4 & 6 & 2 & 7 & 8 & 3 & 0 & 9 \\
6 & 0 & 2 & 4 & 5 & 9 & 3 & 7 & 1 & 8
\end{pmatrix}
\]
}

\noindent\textit{Orthogonality check}: $\cl(L_1^{\mathrm{md48}}, L_2^{\mathrm{md48}}) = 0$ \checkmark.
$\CT = 2$.  This pair's $L_1$ has first row $[8,1,4,2,6,3,0,5,9,7]$, confirming it is not
isotopic to TV-21's $L_1$ (which has first row $[0,9,4,7,6,5,3,8,2,1]$ in reduced form).

\subsection*{A.4\ \ Complete 311-Pair Summary Table}

\begin{center}
{\small
\begin{tabular}{lrrr}
\toprule
Construction & Total & CT=1 & CT=2\\
\midrule
\texttt{tv} (Parker transversal variants) & 56 & 46 & 10\\
\texttt{ic2} (intercalate chain, depth 2) & 3 & 1 & 2\\
\texttt{ic3} (intercalate chain, depth 3) & 3 & 1 & 2\\
\texttt{ic5} (intercalate chain, depth 5) & 5 & 2 & 3\\
\texttt{ic8} (intercalate chain, depth 8) & 4 & 1 & 3\\
\texttt{ic12} (intercalate chain, depth 12) & 3 & 0 & 3\\
\texttt{ic1\_3} (L1 intercalate, depth 3) & 2 & 1 & 1\\
\texttt{ic1\_5} (L1 intercalate, depth 5) & 2 & 1 & 1\\
\texttt{iso} (isotopy transforms) & 6 & 3 & 3\\
\texttt{mdecomp} (transversal decomposition) & 124 & 2 & 122\\
\texttt{sa\_ct} (SA CT-climbing) & 103 & 0 & 103\\
\midrule
\textbf{Total} & \textbf{311} & \textbf{58} & \textbf{253}\\
\bottomrule
\end{tabular}
}
\end{center}

\noindent All 311 pairs are verified MOLS ($\cl = 0$) with CT computed by exhaustive
backtracking.  \textbf{No pair has $\CT > 2$} --- every pair is SAT-certified to have no
orthogonal mate $L_3$ (Glucose3 CDCL, average 42\,ms/pair, 0 timeouts).

% ===========================================================================
\section{Data Tables}\label{app:data}
% ===========================================================================

\subsection*{Sample E=37 Near-Miss Triple}

One of the five $\E=37$ triples (pool entry 0, found 2026-06-07 18:41). All matrices have
symbols $\{0,\ldots,9\}$.

The full triple $(L_1^{(0)}, L_2^{(0)}, L_3^{(0)})$ from pool entry 0. $L_1^{(0)} \perp L_2^{(0)}$
is verified ($\cl_{12}=0$); $L_3^{(0)}$ is a valid Latin square with $\cl_{13}=22$, $\cl_{23}=15$.

{\small
\[
L_1^{(0)} =
\begin{pmatrix}
4 & 7 & 3 & 2 & 8 & 9 & 5 & 1 & 0 & 6 \\
8 & 2 & 5 & 1 & 0 & 4 & 7 & 6 & 3 & 9 \\
1 & 0 & 9 & 3 & 4 & 8 & 6 & 2 & 7 & 5 \\
7 & 3 & 6 & 4 & 5 & 0 & 8 & 9 & 2 & 1 \\
9 & 8 & 4 & 7 & 6 & 1 & 2 & 0 & 5 & 3 \\
3 & 1 & 0 & 5 & 7 & 6 & 9 & 8 & 4 & 2 \\
0 & 4 & 1 & 6 & 2 & 5 & 3 & 7 & 9 & 8 \\
6 & 5 & 2 & 8 & 9 & 7 & 0 & 3 & 1 & 4 \\
5 & 6 & 7 & 9 & 3 & 2 & 1 & 4 & 8 & 0 \\
2 & 9 & 8 & 0 & 1 & 3 & 4 & 5 & 6 & 7
\end{pmatrix}
\qquad
L_2^{(0)} =
\begin{pmatrix}
6 & 0 & 2 & 3 & 5 & 9 & 7 & 1 & 4 & 8 \\
3 & 4 & 6 & 7 & 8 & 5 & 1 & 0 & 9 & 2 \\
2 & 3 & 5 & 1 & 0 & 6 & 9 & 8 & 7 & 4 \\
9 & 8 & 7 & 4 & 2 & 1 & 0 & 3 & 5 & 6 \\
8 & 7 & 9 & 2 & 1 & 4 & 6 & 5 & 3 & 0 \\
5 & 9 & 0 & 8 & 6 & 3 & 4 & 2 & 1 & 7 \\
7 & 2 & 8 & 5 & 9 & 0 & 3 & 4 & 6 & 1 \\
4 & 5 & 1 & 9 & 7 & 8 & 2 & 6 & 0 & 3 \\
1 & 6 & 3 & 0 & 4 & 2 & 5 & 7 & 8 & 9 \\
0 & 1 & 4 & 6 & 3 & 7 & 8 & 9 & 2 & 5
\end{pmatrix}
\]
\[
L_3^{(0)} =
\begin{pmatrix}
6 & 1 & 2 & 0 & 5 & 7 & 3 & 9 & 4 & 8 \\
8 & 9 & 0 & 1 & 6 & 3 & 2 & 7 & 5 & 4 \\
4 & 2 & 3 & 7 & 8 & 1 & 0 & 5 & 6 & 9 \\
9 & 3 & 7 & 5 & 1 & 6 & 4 & 8 & 2 & 0 \\
2 & 0 & 5 & 8 & 3 & 4 & 7 & 1 & 9 & 6 \\
3 & 8 & 9 & 4 & 7 & 5 & 1 & 6 & 0 & 2 \\
0 & 7 & 1 & 9 & 4 & 2 & 6 & 3 & 8 & 5 \\
7 & 6 & 8 & 2 & 9 & 0 & 5 & 4 & 3 & 1 \\
1 & 5 & 4 & 6 & 0 & 9 & 8 & 2 & 7 & 3 \\
5 & 4 & 6 & 3 & 2 & 8 & 9 & 0 & 1 & 7
\end{pmatrix}
\]
}

Verified: $L_1^{(0)} \perp L_2^{(0)}$ ($\cl_{12}=0$); $L_3^{(0)}$ is a valid Latin square;
$\cl_{13}=22$, $\cl_{23}=15$, $\E=37$.  Full data for all five $\E=37$ triples is in
\texttt{mols10/results/near\_miss\_triple.json}.

\subsection*{Notation Summary}

\begin{center}
\begin{tabular}{cl}
\toprule
Symbol & Meaning\\
\midrule
$N(n)$ & Max number of MOLS of order $n$\\
$L_i$ & Latin square $i$ ($10\times10$, symbols $0\mbox{--}9$)\\
$L_i \perp L_j$ & $L_i$ orthogonal to $L_j$\\
$\cl(A,B)$ & Clash count: symbol pairs missing from superposition\\
$\E$ & Total energy $= \cl_{12}+\cl_{13}+\cl_{23}$\\
$\CT(L_1,L_2)$ & Common transversal count\\
SA & Simulated annealing\\
PT & Parallel tempering\\
ILS & Iterated local search\\
CDCL & Conflict-driven clause learning\\
\bottomrule
\end{tabular}
\end{center}

\bigskip
\noindent\textbf{Data availability.} All near-miss triples, MOLS pairs, worker logs, and
search code are available at \url{https://github.com/ajzhanghk/autoresearch-glm}, branch
\texttt{claude/mols-order-10-search-yfQXK}.

% ===========================================================================
\begin{thebibliography}{99}
% ===========================================================================

\bibitem{audemard2009}
Audemard, G., \& Simon, L. (2009).
Predicting learnt clauses quality in modern SAT solvers.
\textit{Proceedings of IJCAI}, 399--404.

\bibitem{ortools}
Google OR-Tools Team. (2023).
OR-Tools: Google's operations research tools.
\url{https://developers.google.com/optimization}.
Version 9.x, CP-SAT solver.

\bibitem{rc2}
Ignatiev, A., Morgado, A., \& Marques-Silva, J. (2019).
RC2: An efficient MaxSAT solver.
\textit{Journal on Satisfiability, Boolean Modeling and Computation}, 11(1), 53--64.

\bibitem{clements1977}
Clements, G. F., \& Lindstr\"{o}m, B. (1965).
A generalization of a combinatorial theorem of Macaulay.
\textit{Journal of Combinatorial Theory}, 7(3), 230--238.

\bibitem{colbourn2006}
Colbourn, C. J., \& Dinitz, J. H. (Eds.) (2006).
\textit{Handbook of Combinatorial Designs} (2nd ed.).
Chapman \& Hall/CRC.

\bibitem{denes1991}
D\'{e}nes, J., \& Keedwell, A. D. (1991).
\textit{Latin Squares: New Developments in the Theory and Applications}.
North-Holland.

\bibitem{euler1782}
Euler, L. (1782).
Recherches sur une nouvelle esp\`{e}ce de quarr\'{e}s magiques.
\textit{Verhandelingen uitgegeven door het zeeuwsch genootschap der wetenschappen te Vlissingen}, 9, 85--239.

\bibitem{geyer1991}
Geyer, C. J. (1991).
Markov chain Monte Carlo maximum likelihood.
\textit{Proceedings of the 23rd Symposium on the Interface}, 156--163.

\bibitem{knuth2000}
Knuth, D. E. (2000).
Dancing links.
\textit{Millennial Perspectives in Computer Science}, 187--214.

\bibitem{lam1989}
Lam, C. W. H., Thiel, L., \& Swiercz, S. (1989).
The nonexistence of finite projective planes of order 10.
\textit{Canadian Journal of Mathematics}, 41(6), 1117--1123.

\bibitem{mckay2007}
McKay, B. D., Meynert, A., \& Myrvold, W. (2007).
Small Latin squares, quasigroups, and loops.
\textit{Journal of Combinatorial Designs}, 15(2), 98--119.

\bibitem{parker1960}
Parker, E. T. (1960).
Orthogonal Latin squares.
\textit{Proceedings of the National Academy of Sciences}, 47(6), 859--862.

\bibitem{tarry1901}
Tarry, G. (1901).
Le probl\`{e}me des 36 officiers.
\textit{Compte Rendu de l'Association Fran\c{c}aise pour l'Avancement des Sciences Naturelles}, 2, 170--203.

\bibitem{wanless2011}
Wanless, I. M., \& Webb, B. S. (2011).
The existence of Latin squares without orthogonal mates.
\textit{Designs, Codes and Cryptography}, 60(2), 143--151.

\end{thebibliography}

\end{document}
