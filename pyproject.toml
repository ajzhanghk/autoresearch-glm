[project]
name = "autoresearch-glm"
version = "0.1.0"
description = "A minimal tabular GLM fork of karpathy/autoresearch"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.0",
    "pandas>=2.0",
    "pyarrow>=15.0",
]
